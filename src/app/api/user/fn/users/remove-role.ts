/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UserResponseDto } from '../../models/user-response-dto';

export interface RemoveRole$Params {
  id: string;
  role: 'USER' | 'ADMIN' | 'SELLER' | 'SUPPORT';
}

export function removeRole(http: HttpClient, rootUrl: string, params: RemoveRole$Params, context?: HttpContext): Observable<StrictHttpResponse<UserResponseDto>> {
  const rb = new RequestBuilder(rootUrl, removeRole.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('role', params.role, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UserResponseDto>;
    })
  );
}

removeRole.PATH = '/api/v1/users/{id}/roles/remove';
